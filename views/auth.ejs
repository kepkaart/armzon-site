<!DOCTYPE html>
<html lang="ru">
<head>
    <%- include('partials/head') %>
    <title>Профиль <%= user.username %> | Armzon RP</title>
</head>
<body>
    <%- include('partials/header') %>

    <div class="container mt-5">
        <div class="card">
            <div class="card-header">
                <h3>Профиль <%= user.username %></h3>
            </div>
            <div class="card-body">
                <p><strong>Уровень:</strong> <%= user.gameStats.level %></p>
                <p><strong>Деньги:</strong> $<%= user.gameStats.money %></p>
                <p><strong>Фракция:</strong> <%= user.gameStats.faction %></p>
                <a href="/settings" class="btn btn-primary">Настройки</a>
                <a href="/logout" class="btn btn-danger">Выйти</a>
            </div>
        </div>
    </div>

    <%- include('partials/footer') %>
</body>
</html>